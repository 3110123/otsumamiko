<h1 class="text-center my-5">タグ編集</h1>
<div class="row justify-content-center text-center">
  <div class="col-8">
    <div class="card text-center">
      <div class="card-body">
      <%= form_with model: @tag, url: admin_tag_path(@tag), local: true, method: :patch do |f| %>
        <div class="form-group">
          <%= f.label :name, class: "text-info" %>
          <%= f.text_field :name, value: @tag.name, class: "form-control" %>
          <%= render 'shared/error_messages', model: f.object, attribute: :name %>
        </div>
        <%= f.submit class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>